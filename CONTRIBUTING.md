# 헥사트라 리포지토리 기여 가이드 
## 0. 용어 
#### 리포지토리 (Repo)
- 문서가 저장된 "프로젝트 아카이브"
#### 포크 (Fork)
- 본인 깃허브 계정으로 리포지토리를 복사해 가져오는 것
- 포크를 통해서 수정 및 추가하고 싶은 문서/내용을 다루고, 이후 원본 리포지토리에 합치는 방식으로 기여 가능.
#### 커밋 (Commit)
- 변경 내용을 저장하는 단위
- 내용을 수정/추가/제거할 때마다 커밋을 통해 그 변경사항을 저장해가는 것.
#### 풀리퀘 (Pull Request)
- 복사해온 리포지토리를 바탕으로 발생한 변경사항을 원본 리포지토리에 반영되길 바랄 때 보내는 요청
## 1. 준비물 
#### GitHub 계정 
## 2. 포크 방법 
#### a. 헥사트라 리포지토리(원본) 페이지로 이동 
  - 좌측 상단의 `<> Code` 를 누르면 리포지토리 페이지로 이동합니다.
#### b. 우측 상단 Fork 버튼 클릭 
<img width="1410" height="341" alt="image" src="https://github.com/user-attachments/assets/9fa82a2c-5909-4eb4-81e3-45dd2691a0f5" /> <br>

#### c. Create fork 를 클릭해 복사된 리포지토리를 본인 계정에 생성

## 3. 포크(복사한 리포지토리)와 원본 리포지토리의 동기화 
- 포크는 원본 리포지토리가 이후 단독적으로 내용이 업데이트되어도 자동으로 같이 업데이트되지 않습니다.
- 그에 따라, 별도로 포크에서 **문서 수정/추가 작업을 하기에 앞서 반드시** 현재 원본 리포지토리와의 동기화를 해주셔야 합니다. 
- 윗 과정을 하지 않은 채 문서 수정/추가 작업을 하시게 될 경우 원본 리포지토리와 충돌이 발생하여, 이 경우 풀리퀘(Pull Request) 확인이 지연되거나 거절 될 수 있습니다.
#### a. 본인 계정의 포크(복사한 리포지토리)로 가서 우측 상단 즈음에 위치한 `Sync Fork` 버튼을 눌러주고 이후에 나타나는 승인 버튼을 눌러줍니다.
#### b. 동기화가 정상적으로 되었다면 더 이상 포크 우측 상단에 `Sync Fork`라는 버튼이 보이지 않을겁니다. 

## 4. 문서 수정 작업하기 
> 전제 1) 모든 문서 수정/추가 작업은 본인 게정에 생성된 포크(복사된 리포지토리)를 통해 이뤄져야합니다.
> 전제 2) 모든 문서 수정/추가 작업에 앞서 Sync Fork를 통해 포크와 원본 리포지토리를 동기화시켜야합니다. 
#### a. 수정하고자 하는 문서를 클릭해 들어갑니다. 
#### b. 문서의 우측 상단에 위치한 `연필`을 클릭해 수정 작업을 할 수 있습니다.
- 수정 작업 창에 들어온 이후, 상단의 `Edit` 은 현재 내용을 수정할 수 있는 창이, `Preview`는 내용이 있는 창이 실제 뷰어에서 어떻게 보이는지 표시해줍니다.  
#### c. 문서의 수정 작업이 완료된 이후 문서의 우측 상단에 위치한 `Commit changes...` 를 눌러 저장합니다. 
  - Commit Change 를 누르면 추가적인 창이 뜨는데, 그 추가적인 창에 있는 Commit Change 를 누르면 최종적으로 저장합니다.
  - 나타나는 추가적인 창을 통해 수정사항에 대한 코멘트를 남길 수 있습니다.

## 5. 문서 추가 작업하기 
> 전제 1) 모든 문서 수정/추가 작업은 본인 게정에 생성된 포크(복사된 리포지토리)를 통해 이뤄져야합니다.
> 전제 2) 모든 문서 수정/추가 작업에 앞서 Sync Fork를 통해 포크와 원본 리포지토리를 동기화시켜야합니다.
#### a. 문서를 추가하고자 하는 경로의 폴더로 이동합니다. 
- 만일 새로운 '폴더'를 제작하고 싶다면 폴더를 제작하고자 하는 경로의 폴더에서, **b.** 의 과정까지 한 후, 제목을 지으실 때...
<img width="989" height="95" alt="image" src="https://github.com/user-attachments/assets/3cf24459-a5c3-4767-b6f6-aa63544b2056" /> <br>

- 위와 같이 폴더명을 입력한 상태에서 뒤에 `/` 를 입력해주면,
<img width="978" height="92" alt="image" src="https://github.com/user-attachments/assets/be290e31-e37f-4b29-811d-b46caf1c073e" /> <br>

- 폴더가 생성되어 이후 **c.** 대로 제목을 작성해주면 됩니다. 

#### b. 우측 상단에 있는 `Add file` 을 클릭하고, `Create new file` 까지 눌러줍니다. 
- 혹은 `Upload files`를 통해 기존에 작성한 문서를 그대로 가져다 올리는 것도 가능합니다.

#### c. 상단에 문서 제목을 작성해줍니다.
- 마크다운 형식의 파일 생성을 희망 할 경우 반드시 제목의 맨 뒤에 `.md` 가 입력되어 있어야합니다.
<img width="975" height="142" alt="image" src="https://github.com/user-attachments/assets/89fce8bf-f23a-4abc-bf2d-cdadf092e82c" /> <br>

#### d. 입력하고자 하는 내용을 입력합니다. 
- 보통은 포스타입/노션 등 외부 플랫폼에서 작성해 복사해온 열람 링크를 붙여넣기 하시기만 해도 될겁니다.
<img width="962" height="240" alt="image" src="https://github.com/user-attachments/assets/ce7c7704-3697-4819-82c0-5e6e868bdb10" /> <br>

- 마크다운 파일에 바로 내용을 작성하는 것도 가능합니다. 마크다운 문법/사용법은 인터넷에서 찾아보시면 친절히 안내된 설명서가 많으니 참고하시면 됩니다.

#### e. 문서의 수정 작업이 완료된 이후 문서의 우측 상단에 위치한 `Commit changes...` 를 눌러 저장합니다. 
  - Commit Change 를 누르면 추가적인 창이 뜨는데, 그 추가적인 창에 있는 Commit Change 를 누르면 최종적으로 저장합니다.
  - 나타나는 추가적인 창을 통해 수정사항에 대한 코멘트를 남길 수 있습니다.
<img width="591" height="614" alt="image" src="https://github.com/user-attachments/assets/2075b3b1-cf6d-46d4-9187-3f1a94139f38" /> <br>

## 6. 풀리퀘 보내기 (Pull Request)
- 포크에서 수정/추가 작업을 하신 후, 그 변경사항을 원본 리포지토리에 반영되도록 하기 위해서는 `Pull Request` (이하 풀리퀘로 표기) 를 열어야합니다.
- 보통 문서를 수정하고 난 뒤에 포크 상단의 `<> Code` 버튼을 눌러 메인 화면으로 돌아오시면 `Open Pull Request` 관련 버튼이 보이실겁니다.
- 그 버튼을 누른 다음, 풀리퀘와 함께 전송될 게시글을 쓸 수 있는 창이 뜰텐데, 그 창에서 아래와 같은 내용들을 자유롭게 작성해주시면 됩니다.
  - 어떠한 변경사항이 있었는지...
  - 변경사항이 생기게 된 사유...
- 풀리퀘를 성공적으로 열게 된다면, 이후 원본 리포지토리의 관리자가 확인 후 승인하게 된다면 해당 수정/변경사항이 원본 리포지토리에 반영됩니다. 이로써, 해당 커뮤니티 깃허브로의 직접적인 기여 과정 하나가 마무리됩니다.     

